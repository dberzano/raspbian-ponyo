---
- name: Update list of packages
  apt: cache_valid_time=43200 update_cache=yes

- name: Install base packages
  apt:
    state: present
    name:
      - autoconf
      - automake
      - avahi-daemon
      - bluealsa
      - build-essential
      - chrony
      - git
      - libasound2-dev
      - libavahi-client-dev
      - libconfig-dev
      - libdaemon-dev
      - libpopt-dev
      - libpulse-dev
      - libsoxr-dev
      - libssl-dev
      - libtool
      - python-dbus
      - python3-klein
      - vim-nox
      - xmltoman
